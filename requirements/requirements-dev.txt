# Test dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0

# Linting and code quality
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0
mypy>=1.0.0

# Documentation
sphinx>=6.0.0
sphinx-rtd-theme>=1.2.0

# Development tools
pre-commit>=3.0.0
ipython>=8.0.0
jupyter>=1.0.0

# Test coverage
coverage>=7.0.0
# codecov 3.x is not available on PyPI; pin to latest 2.x
codecov==2.1.13

# Optional: Distributed/ZeRO optimizer runtime (GPU builds require matching CUDA)
# Install only if needed; otherwise trainer falls back to DDP/single-process.
deepspeed
